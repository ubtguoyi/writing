/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      loaders: {
        // Add any turbo loaders here if needed
      }
    }
  }
};

export default nextConfig;
